# create a new list called lower_colors, where each color in colors is lower cased.


colors = ['Red', 'Blue', 'Green', 'Purple']
lower_colors = [ ]

for color in colors:
    lower_colors.append(color.lower())
print(lower_colors)
