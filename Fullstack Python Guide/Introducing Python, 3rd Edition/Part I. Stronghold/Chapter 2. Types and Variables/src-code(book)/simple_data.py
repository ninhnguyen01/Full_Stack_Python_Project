# variable = value

# Note
# This = is not expressing equality, like a formula in algebra. 
# Maybe computer languages could have used a different symbol for assignment than = (and some do), 
# but assignment is so common in computing that it’s established itself. You quickly get used to it.

x = 5
print("What value is x? It is", x)
