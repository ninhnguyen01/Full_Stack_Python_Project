# Make yet another class called, of course, Thing3. This time, assign the value xyz to an instance (object) attribute called letters. Print letters.
class Course():
    value = "xyz"

letter = Course()
print(letter.value)
