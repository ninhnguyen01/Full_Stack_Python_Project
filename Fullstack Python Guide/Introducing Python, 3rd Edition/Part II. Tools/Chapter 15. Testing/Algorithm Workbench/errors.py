# Find out what’s wrong with the code. Use Pylint and Ruff (install them if needed).
import math as m # import math module with alias

# print out the square root of value using math module
NUM = 9
print(f"square root of {NUM} is", m.sqrt(NUM))
